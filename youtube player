// ==========================================
// Author  : If you like content like this, you can join this channel. 📲
// Contact : https://t.me/jieshuo_materials
// ==========================================

require "import" import "android.widget.*" import "android.view.View" import "android.media.*" import "android.net.Uri" import "android.content.*" import "android.speech.*" import "android.text.*" import "cjson" import "android.app.*" import "android.os.*" import "java.util.Arrays"  activity = this  local timeHandler = Handler(Looper.getMainLooper()) local timeRunnable  local currentMediaPlayer = nil  local isModeVideo = false  local audioManager = activity.getSystemService(Context.AUDIO_SERVICE) local focusRequest = nil local audioFocusChangeListener  local currentSpeed = 1.0 local currentPitch = 1.0  local currentVolumeBoost = 1.0  local defaultQuality = "720"  local currentSortOrder = "default" local currentFilter = "none"  local isLooping = false local isAutoNext = true local autoShowSearch = true  local MAX_SEEKBAR_VALUE = 300 local SEEKBAR_STEP = 10 local QUALITIES = {"360", "480", "720", "1080"}  local PREFS_NAME = "PlaybackSettings" local prefs = activity.getSharedPreferences(PREFS_NAME, Context.MODE_PRIVATE)  local videoQueue = {}  local currentResults = nil local currentDisplayResults = {} local currentPlayingIndex = nil local currentSelectedURL = nil local currentSelectedQuality = nil  local MAX_HISTORY = 10 local searchHistory = {}  function loadHistory() 	local historyJson = prefs.getString("searchHistory", "[]") 	local status, history = pcall(cjson.decode, historyJson) 	if status and type(history) == "table" then 		searchHistory = history 	else 		searchHistory = {} 	end end  function saveHistory() 	local editor = prefs.edit() 	editor.putString("searchHistory", cjson.encode(searchHistory)) 	editor.apply() end  function addSearchHistory(query) 	if not query or #query == 0 then return end  		for i = #searchHistory, 1, -1 do 			if searchHistory[i] == query then 				table.remove(searchHistory, i) 			end 		end  		table.insert(searchHistory, 1, query)  		while #searchHistory > MAX_HISTORY do 			table.remove(searchHistory) 		end 		saveHistory() 	end  	function loadSettings() 		currentSpeed = prefs.getFloat("speed", 1.0) 		currentPitch = prefs.getFloat("pitch", 1.0) 		currentVolumeBoost = prefs.getFloat("volumeBoost", 1.0) 		defaultQuality = prefs.getString("defaultQuality", "720")  		isLooping = prefs.getBoolean("isLooping", false) 		isModeVideo = prefs.getBoolean("isModeVideo", false) 		isAutoNext = prefs.getBoolean("isAutoNext", true) 		autoShowSearch = prefs.getBoolean("autoShowSearch", true)  		loadHistory() 	end  	function saveSettings() 		local editor = prefs.edit() 		editor.putFloat("speed", currentSpeed) 		editor.putFloat("pitch", currentPitch) 		editor.putFloat("volumeBoost", currentVolumeBoost) 		editor.putString("defaultQuality", defaultQuality)  		editor.putBoolean("isLooping", isLooping) 		editor.putBoolean("isModeVideo", isModeVideo) 		editor.putBoolean("isAutoNext", isAutoNext) 		editor.putBoolean("autoShowSearch", autoShowSearch)  		editor.apply() 	end  	function vibrate() 		local vibrator = activity.getSystemService(Context.VIBRATOR_SERVICE) 		if vibrator and vibrator.hasVibrator() then 			vibrator.vibrate(35) 		end 	end  	function requestAudioFocus() 		if audioManager == nil then return AudioManager.AUDIOFOCUS_REQUEST_FAILED end  			local audioAttributes = AudioAttributes.Builder() 			.setUsage(AudioAttributes.USAGE_MEDIA) 			.setContentType(AudioAttributes.CONTENT_TYPE_MUSIC) 			.build()  			if audioFocusChangeListener == nil then 				audioFocusChangeListener = AudioManager.OnAudioFocusChangeListener { 					onAudioFocusChange = function(focusChange) 					timeHandler.post(Runnable { run = function() 							if currentMediaPlayer == nil then return end  								if focusChange == AudioManager.AUDIOFOCUS_LOSS_TRANSIENT or 									focusChange == AudioManager.AUDIOFOCUS_LOSS_TRANSIENT_CAN_DUCK or 									focusChange == AudioManager.AUDIOFOCUS_LOSS then 									if currentMediaPlayer and currentMediaPlayer.isPlaying() then 										videoPlayer.pause() 										playPauseButton.text = "Play" 										service.speak("Audio focus lost, paused.") 									end 								elseif focusChange == AudioManager.AUDIOFOCUS_GAIN then 									if currentMediaPlayer and not currentMediaPlayer.isPlaying() and (playPauseButton.text == "Play") then 										videoPlayer.start() 										playPauseButton.text = "Pause" 										applyPlaybackParams(currentSpeed, currentPitch) 										applyVolumeBoost(currentVolumeBoost) 										service.speak("Audio focus restored, resuming playback.") 									end 								end 							end}) 				end 				} 		end  		focusRequest = AudioFocusRequest.Builder(AudioManager.AUDIOFOCUS_GAIN) 		.setAudioAttributes(audioAttributes) 		.setOnAudioFocusChangeListener(audioFocusChangeListener) 		.build()  		local result = audioManager.requestAudioFocus(focusRequest) 		return result 	end  	function abandonAudioFocus() 		if audioManager and focusRequest then 			audioManager.abandonAudioFocusRequest(focusRequest) 		end 	end  	function applyPlaybackParams(speed, pitch)  		abandonAudioFocus()